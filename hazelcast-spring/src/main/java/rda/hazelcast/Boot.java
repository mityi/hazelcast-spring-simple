package rda.hazelcast;

import org.springframework.boot.SpringApplication;

public class Boot {

    public static void main(String[] args) throws Exception {
        SpringApplication.run(rda.hazelcast.AppConfig.class, args);
    }
}
